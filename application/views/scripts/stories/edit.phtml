<div id="content">
    <div id="content-wrap">

        <div id="title-bar">
            <h1>Edit Story</h1>
        </div>
        <?php
        if ( !is_null( $this->message ) && !empty( $this->message ) )
        {
            ?>
            <div id="flashmessage" class="flashmessage">
                <ul>
                    <li><?= $this->message; ?></li>
                </ul>
            </div>
            <?php
        }
        ?>

        <div id="edit-story-form">
            <?php echo $this->form; ?>
        </div>
    </div>
</div>

<script type="text/javascript">
    $( document ).ready( function () {

        $( "#save" ).on( 'click', function ( eventData )
        {
            new Confirm( 'Confirm', 'Are you sure you want to save the story details?', 'Yes', function () {
                $( "#edit-story" ).submit();
            } );
            eventData.preventDefault();
        } );

    } );
</script> 